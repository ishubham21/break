import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#23272A',
      contrastText: '#fff',
    },
    secondary: {
      main: '#5663F7',
      contrastText: '#fff',
    },
  },
});

export default theme